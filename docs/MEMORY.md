# MEMORY

2026-03-06 00:30 +0000 | decision | MEM-0142 | ui,office,objects,invariant | Office object interactions are metadata-driven and panel-first: builder mode exclusively owns object transform/config controls, normal mode clicks must open configured object panels instead of builder menus, and embed-backed panels must provide an external-open fallback when iframe embedding is blocked.
2026-03-06 00:00 +0000 | decision | MEM-0141 | earth-watch,skills,heartbeat,invariant | Earth-watch MVP is single-agent and deterministic-first: one heartbeat-driven OpenClaw agent owns watchlist memory and orchestration, while the EO script stays stateless and only accepts analysis inputs (`mode`, `bbox`, date windows, artifact dir) before returning structured evidence and artifact paths.
2026-03-05 16:20 +0000 | decision | MEM-0140 | convex,cli,status,timeline,invariant | Agent status/activity observability is unified: ShellCorp stores status and timeline breadcrumbs in Convex `agentEvents` (with `activityType` metadata) and derives head bubbles via `agentStatus`; `teamActivityEvents` is removed, and the primary agent-facing command is `shellcorp status` with env-resolved agent/team context.
2026-03-05 15:00 +0000 | decision | MEM-0139 | affiliate,skills,team-panel,invariant | SC11 demo skill operations are team-to-agent synchronized: saving business slot skills must support deterministic PM/executor equip sync into `openclaw.json` agent `skills[]`, Team Panel must expose per-member equipped skill visibility, and demo seeding must include pipeline tasks + activity breadcrumbs + artifact references for operator walkthroughs.
2026-03-05 12:40 +0000 | decision | MEM-0138 | ui,timeline,heartbeat,audit,invariant | Agent observability is beat-centric and audit-durable: ShellCorp must retain Convex `agentEvents` without scheduled pruning, propagate optional `beatId` through board/activity/status pipelines, and expose drill-down timeline views that separate recency-only head bubbles from full historical timeline inspection.
2026-03-05 12:40 +0000 | decision | MEM-0137 | ui,chat,history,invariant | OpenClaw chat parity requires history-first tool observability: ShellCorp must preserve `chat.history` tool records (`role=toolResult` and assistant `toolCall` content) as `working_output` messages so tool execution remains visible after reload under the working-output toggle without additional backend storage.
2026-03-05 12:05 +0000 | decision | MEM-0136 | ui,artefacts,projects,invariant | Project artefact inspection is Team Panel project-scoped: operators open generated workspace files from Projects tab (agent-grouped, read-only preview, partial-failure tolerant) using existing `agents.files.*` gateway methods instead of terminal-only inspection.
2026-03-05 10:25 +0000 | decision | MEM-0135 | ui,chat,observability,invariant | Working-output chat visibility is event-first: `agent` stream events must not be dropped when `sessionKey` is absent, session matching must tolerate full-key vs short-key representations, and run mismatches may be shown while the active chat run is still streaming so operators can audit intermediate tool execution in real time.
2026-03-05 06:10 +0000 | decision | MEM-0134 | heartbeat,status,ops,invariant | Heartbeat proof runs are standardized through `scripts/heartbeat-smoke.sh`: each targeted agent must emit both `STATUS:` and `HEARTBEAT_OK`, the script must exit non-zero on missing markers, and operators should use this command as the canonical regression check after heartbeat/bootstrap changes.
2026-03-05 05:00 +0000 | decision | MEM-0133 | affiliate,spec,skills,invariant | Affiliate marketing MVP execution is standardized by `SC11`: PM and executor loops must run via board-driven task routing (`research -> create_content -> distribute -> measure`) with explicit spend/revenue logging and concrete skill contracts (inference.sh generation, TikTok/Instagram distribution, Amazon dashboard measurement).
2026-03-05 04:20 +0000 | decision | MEM-0132 | convex,team-timeline,invariant | Team and agent timeline observability requires first-class `teamId` in Convex status/board events: new writes must carry `teamId`, timeline queries should prefer team-scoped indexes, and reads remain tolerant of legacy rows missing `teamId` during rollout.
2026-03-05 04:10 +0000 | decision | MEM-0131 | business,ledger,finance,invariant | Team finance is first-class and append-only at project scope: canonical balance lives in `project.account.balanceCents`, canonical transaction history lives in `project.accountEvents[]` (`credit|debit`), and legacy `ledger[]` remains compatibility/audit mirror for revenue-cost views.
2026-03-05 03:25 +0000 | decision | MEM-0130 | ui,kanban,cli,permissions,invariant | Team panel mission-control phase is internal-board-first: Kanban UX drops federation controls in favor of internal task operations, while CLI/HTTP board mutations require explicit permission checks and support full task lifecycle (`add/update/move/assign/block/done/reopen/reprioritize/delete`) plus activity status logging.
2026-03-04 22:10 +0000 | decision | MEM-0129 | convex,kanban,team-board,invariant | Business team execution boards are Convex-canonical: task state and activity logs now live in `teamBoardTasks`/`teamBoardEvents`/`teamActivityEvents`, while sidecar tasks are non-canonical compatibility state.
2026-03-04 19:30 +0000 | decision | MEM-0128 | status,convex,heartbeat,invariant | Agent status visibility is self-report-first: operators rely on explicit agent writes to Convex (`/status/report` -> `status_report` events) for progress state, while `shellcorp-status` message hook inference is diagnostics-only and disabled by default.
2026-03-04 17:25 +0000 | decision | MEM-0127 | cli,office,doctor,invariant | Office object sidecar hygiene must be operator-auditable: `office doctor` reports invalid objects and `office doctor --fix` removes entries that cannot render or map correctly (for example custom meshes without mesh paths, or team clusters targeting missing/archived projects).
2026-03-04 17:10 +0000 | decision | MEM-0126 | cli,office,ui,invariant | Office CLI placement must preserve UI renderability and team semantics: `custom-mesh` placement requires mesh path metadata, and `team-cluster` placement must resolve to a real/revived project-backed `team-<projectId>` mapping so clusters render as actual teams in panel flows.
2026-03-04 16:45 +0000 | decision | MEM-0125 | cli,office,placement,invariant | Office object placement commands must be collision-safe and deterministic: `office add` accepts exactly one of `--position` or `--auto-place`, auto placement scans bounded grid slots in deterministic order, and both add/move reject occupied destinations using shared XZ footprint checks.
2026-03-04 16:15 +0000 | decision | MEM-0124 | cli,openclaw,agents,invariant | Team agent creation must provision OpenClaw runtime state in addition to sidecar metadata: create/update `openclaw.json` `agents.list`, initialize `workspace-<agentId>` bootstrap files, initialize `agents/<agentId>/sessions`, and keep business HEARTBEAT templates in the canonical agent workspace path.
2026-03-04 13:05 +0000 | decision | MEM-0123 | cli,business,resources,invariant | Business/resource operations are CLI-complete: operators must be able to read (`team business get`, `team resources list/events`), mutate (`set/refresh/reserve/release/remove`), preview (`team heartbeat render`), and validate (`doctor team-data`) without direct sidecar JSON edits.
2026-03-04 10:45 +0000 | decision | MEM-0121 | business,autonomy,invariant | Autonomous business teams use fixed role split (`biz_pm` + `biz_executor`) with capability-slot business adapters (`measure`, `execute`, `distribute`) and sidecar-native `ledger[]/experiments[]/metricEvents[]` tracking per project.
2026-03-04 12:10 +0000 | decision | MEM-0122 | resources,autonomy,invariant | Business teams track advisory resources (`cash_budget`, `api_quota`, `distribution_slots`, `custom`) via `resources[]/resourceEvents[]`; heartbeat planning must include resource snapshot and low-resource advisories without hard execution blocking in MVP.
2026-03-04 03:05 +0000 | decision | MEM-0120 | cli,office,personalization,invariant | Office personalization is CLI-first via `shellcorp office` commands (`print/list/teams/add/move/remove/generate/theme`) over sidecar state, and meshy creation starts as spec capture under `~/.openclaw/assets/mesh` before optional custom-mesh placement.
2026-03-04 01:40 +0000 | decision | MEM-0119 | cli,topology,invariant | CEO team management is CLI-first via `shellcorp` commands that mutate `~/.openclaw/company.json` (and optional `office-objects.json` cluster metadata), with docs-only SCL intent-to-command mapping for agent execution.
2026-02-25 20:10 +0800 | decision | MEM-0100 | architecture,invariant | OpenClaw is the runtime source of truth; Shell Company focuses on UI mapping, visualization, and operator workflows.
2026-02-25 20:10 +0800 | decision | MEM-0101 | docs,invariant | Canonical docs indexes for this project are OpenClaw Multi-Agent Routing and OpenClaw Plugins pages.
2026-02-25 20:15 +0800 | decision | MEM-0102 | plugins,invariant | Notion integration is delivered as an in-repo OpenClaw extension (`extensions/notion`) instead of internal gateway code.
2026-02-25 20:18 +0800 | decision | MEM-0103 | ui,invariant | UI must preserve office/game visualization and only replace the data layer with OpenClaw state adapters.
2026-02-26 00:10 +0800 | decision | MEM-0104 | topology,invariant | Company/org metadata is stored in sidecar JSON (`workspace/office/company.json`) while OpenClaw runtime config contains only active agents/bindings.
2026-02-26 00:10 +0800 | decision | MEM-0105 | routing,invariant | Customer channel routing defaults to project PM agent, with optional per-channel agent override and reconciliation warnings for unmapped targets.
2026-02-26 04:40 +0800 | decision | MEM-0106 | ui,navigation,invariant | Office workspace navigation is tab-first (`team`, `session`, `skills`, `settings`) with a single app-store state source so scene interactions and settings modal always resolve to one active workspace context.
2026-02-26 05:55 +0800 | decision | MEM-0107 | ui,parity,invariant | For parity-critical workflows (chat, computer view, manage agent, training), ShellCorp uses dedicated Zanarkand-style modals with one-trigger-to-one-modal mapping, while OpenClaw integration is provided by thin frontend compatibility hooks until full backend parity is complete.
2026-02-26 06:35 +0800 | decision | MEM-0108 | ui,interactions,invariant | Employee radial Chat must always open ChatDialog above scene/context overlays, employee task routing must open Team Kanban filtered by responsible agent, and team-cluster clicks must resolve to Team Panel open state (not orphan store flags).
2026-02-26 07:20 +0800 | decision | MEM-0109 | ui,panels,invariant | Navigation is panel-first (no OfficeWorkspace overlay): top-left menu launches global Team/Agent Session/Skills panels and Settings option, while team/employee in-scene actions open filtered panel contexts for the selected entity.
2026-02-26 18:05 +0800 | decision | MEM-0110 | memory,invariant,openclaw | Agent memory inspection is file-backed per agent workspace (`MEMORY.md` + `memory/*.md`) and exposed in UI through employee-triggered Agent Memory Panel tabs (`list`, `search`, `graph-placeholder`) using `/openclaw/agents/:agentId/memory-entries`.
2026-02-26 23:10 +0800 | decision | MEM-0111 | kanban,federation,invariant | Kanban federation uses canonical-provider-per-project ownership; ShellCorp acts as unified operational read/orchestration layer rather than global multi-master write owner.
2026-02-26 23:10 +0800 | decision | MEM-0112 | lifecycle,session,invariant | Ticket lifecycle is modeled as session lifecycle (`ticket == session`) until explicit close/reopen transition with auditable state changes.
2026-02-26 23:10 +0800 | decision | MEM-0113 | context-tools,invariant | Provider context indexing and generated tool/skill catalogs must be deterministic by schema version and exposed through OpenClaw-compatible plugin/skill contracts.
2026-02-26 23:10 +0800 | decision | MEM-0114 | autonomy,heartbeat,invariant | Autonomous work loops require explicit heartbeat policy and operator governance controls (pause/resume/manual run/circuit visibility) with per-beat audit records.
2026-02-26 22:45 +0800 | decision | MEM-0115 | kanban,federation,invariant | SC06 baseline enforces internal canonical provider default with explicit per-project canonical override, visible sync health, and manual resync controls in Team Kanban.
2026-02-26 22:45 +0800 | decision | MEM-0116 | notion,skills,invariant | Notion workspace personalization is captured as project-scoped provider index profiles (entity id, field mapping, command hints, naming prefix) to stabilize generated context-tool metadata.
2026-02-26 22:35 +0800 | decision | MEM-0115 | office,placement,invariant | Office object persistence must reconcile UI-facing IDs (`office-*`) with sidecar IDs before write/delete operations, and draggable object local transforms must rehydrate from canonical adapter props when not dragging so reload always reflects persisted coordinates.
2026-02-26 23:55 +0800 | decision | MEM-0117 | notion,webhook,invariant | Notion inbound automation is comments-first via OpenClaw mapped hooks (`/hooks/notion`) with transform-based filtering; a temporary probe endpoint is acceptable only for verification/payload discovery and must be hot-swapped out once mapping is validated.
